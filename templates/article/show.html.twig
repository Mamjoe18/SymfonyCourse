{% extends "base.html.twig" %}

{% block title %}
    {{ article.title }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="ad-title-sym" style="background-image: url({{ article.coverImage }})">
        <div class="container">
            <h1>{{ article.title }}</h1>
            <p>{{ article.shortDescription }}</p>
            <p class="h2">
                <strong>{{ article.price }}&euro;</strong>
            </p>
            <a class="btn btn-primary" href="#">Acheter</a>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ article.detailedDescription | raw }}

                <div class="carousel slide" data-ride="carousel" id="carouselExampleIndicators">
                    <ol class="carousel-indicators">
                        {% for image in article.images %}
                            <li class="{% if loop.first %}active{% endif %}" data-slide-to="{{ loop.index0 }}" data-target="#carouselExampleIndicators"></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for image in article.images %}
                            <div class="carousel-item {% if loop.first %}active{% endif %}">
                                <img alt="..." class="d-block w-100" src="{{ image.url }}">
                                <div class="carousel-caption d-none d-md-block">
                                    <h5>{{ article.title }}</h5>
                                    <p>{{ image.caption }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" data-slide="prev" href="#carouselExampleIndicators" role="button">
                        <span aria-hidden="true" class="carousel-control-prev-icon"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" data-slide="next" href="#carouselExampleIndicators" role="button">
                        <span aria-hidden="true" class="carousel-control-next-icon"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            {# <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img alt="Avatar de Joseph" src="http://placehold.it/64x64"></div>
                    <div class="col">
                        <h3>Joseph Dupont</h3>
                        <span class="badge badge-primary">3 annonces</span>
                    </div>
                </div>
                <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Iure nisi in vel suscipit, aliquid iusto enim cumque neque excepturi pariatur?</p>
                <p>Ducimus tempora animi accusamus error libero quam impedit et. Cum atque hic rerum dolore voluptas officiis expedita nulla. Rerum, dignissimos.</p>
                <p>Alias fugiat tempore, fugit aut quisquam facilis temporibus, omnis, praesentium architecto molestias quod reiciendis unde possimus vel cum rem laboriosam.</p>
            </div> #}
        </div>
    </div>
</div>

{% endblock %}
