{% extends 'base.html.twig' %}

{% block title %}Nos articles{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="my-3">Voici tous les articles disponibles</h1>
    <div class="row">
        {% for article in articles %}
            <div class="col-md-4">
                <div class="card bg-light mb-3">
                    <div class="card-header text-center">
                        <strong>{{ article.price }}&euro;</strong>
                        <br>
                        <small>Pas encore noté</small>
                    </div>

                    <img src="{{ article.coverImage }}" alt="Image de l'appartement" style="height: 200px; width: 100%; display: block">

                    <div class="card-body">
                        <h4 class="card-title">{{ article.title }}</h4>
                        <p class="card-text">
                            {{ article.shortDescription }}
                        </p>
                        <div class="text-center">
                            <a href="{{ path('article_show', {'slug': article.slug } )}}" class="btn btn-secondary">En savoir plus</a>
                        </div>
                    </div>

                </div>
            </div>
        {% endfor %}
    </div>

    <div class="d-flex justify-content-center mt-5">
        <ul class="pagination">
            <li class="page-item {% if page == 1 %}disabled{% endif %}">
                <a class="page-link" href="{{ path("article_index", {'page': page - 1 }) }}">&laquo;</a>
            </li>

            {% for i in 1..paginate %}
                <li class="page-item {% if page == i %}active{% endif %}">
                    <a class="page-link" href="{{ path("article_index", {'page': i}) }}">{{ i }}</a>
                </li>
            {% endfor %}

            <li class="page-item {% if page == paginate %}disabled{% endif %}">
                <a class="page-link" href="{{ path("article_index", {'page': page + 1 }) }}">&raquo;</a>
            </li>
        </ul>
    </div>

    </div>
{% endblock %}
